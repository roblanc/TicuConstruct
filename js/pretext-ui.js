import{prepare,walkLineRanges}from'./pretext.js';export function shrinkwrap(el,maxAvailableWidth=800){const text=el.innerText.trim();const style=window.getComputedStyle(el);const font=`${style.fontWeight} ${style.fontSize} ${style.fontFamily}`;const prepared=prepare(text,font);let tightestWidth=0;walkLineRanges(prepared,maxAvailableWidth,(line)=>{if(line.width>tightestWidth)tightestWidth=line.width;});el.style.width=`${Math.ceil(tightestWidth + 2)}px`;el.style.maxWidth='100%';}
export function balance(el,containerWidth=1200){const text=el.innerText.trim();const style=window.getComputedStyle(el);const font=`${style.fontWeight} ${style.fontSize} ${style.fontFamily}`;const prepared=prepare(text,font);shrinkwrap(el,containerWidth);}
export function initPretext(){const shrinkElements=document.querySelectorAll('[data-pretext-shrinkwrap]');shrinkElements.forEach(el=>{const container=el.parentElement;const width=container?container.clientWidth:800;shrinkwrap(el,width);});const balanceElements=document.querySelectorAll('[data-pretext-balanced]');balanceElements.forEach(el=>{const container=el.parentElement;const width=container?container.clientWidth:1200;balance(el,width);});let debounce;window.addEventListener('resize',()=>{clearTimeout(debounce);debounce=setTimeout(()=>{initPretext();},150);});}